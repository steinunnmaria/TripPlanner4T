package com.example.tripplanner.Comparators;

import com.example.tripplanner.Classes.DayTrip;

import java.util.Comparator;

public class DayTripDifficultyComparator implements Comparator<DayTrip> {
    public int compare(DayTrip o1, DayTrip o2) {
        return 0;
    }
   /* @Override
    public int compare(DayTrip o1, DayTrip o2) {
        //int a = o1.getDifficulty();
        //int b = o2.getDifficulty();
        return a > b ? +1 : a < b ? -1 : 0;
    }*/
}
